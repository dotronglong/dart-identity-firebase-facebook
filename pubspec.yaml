name: identity_firebase_facebook
description: Facebook Login by Firebase for Identity package
version: 0.3.0
homepage: https://github.com/dotronglong/dart-identity-firebase-facebook

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  identity: ^0.3.0
  identity_firebase: ^0.3.0
  firebase_auth: ^0.15.3
  flutter_facebook_login: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  assets:
    - images/facebook.png